import {Button, Card, CardActions, CardContent, CardMedia, Container, Grid, Typography, TextField, Checkbox, Box, Fab} from "@material-ui/core"

import AddIcon from '@material-ui/icons/Add';
import CancelIcon from '@material-ui/icons/Cancel';

import {animated} from 'react-spring'

export const AnimatedAddIcon = animated(AddIcon)

export const AnimatedCancelIcon = animated(CancelIcon)

export const AnimatedCard = animated(Card);

export const AnimatedButton = animated(Button);

export const AnimatedCardActions = animated(CardActions);

export const AnimatedCardContent = animated(CardContent);

export const AnimatedCardMedia = animated(CardMedia);

export const AnimatedContainer = animated(Container);

export const AnimatedGrid = animated(Grid);

export const AnimatedTypography = animated(Typography);

export const AnimatedTextField = animated(TextField);

export const AnimatedCheckbox = animated(Checkbox);

export const AnimatedBox = animated(Box);

export const AnimatedFab = animated(Fab);